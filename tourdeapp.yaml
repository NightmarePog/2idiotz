# $schema: https://tourde.cloud/api/v2/schema
exposedPort: 80
containers:
  - name: web
    image: registry.tourde.cloud/app_build_repl_idiot/web
    port: 3001
    env:
      HOST: "0.0.0.0"
      PORT: "3001"
      PROTOCOL_HEADER: x-forwarded-proto
      HOST_HEADER: x-forwarded-host
  - name: server
    image: registry.tourde.cloud/app_build_repl_idiot/server
    port: 8080
    env:
      PORT: "8080"
  - name: caddy
    image: registry.tourde.cloud/app_build_repl_idiot/caddy
    port: 80
    env:
      PUBLIC_SCHEME: https
build:
  - name: web
    context: ./frontend
    dockerfile: ./frontend/Dockerfile
  - name: server
    context: ./backend
    dockerfile: ./backend/Dockerfile
  - name: caddy
    context: ./caddy
    dockerfile: ./caddy/Dockerfile
